import React from 'react';
import classes from '../../styles/components/UI/Spinner.module.css';

const Spinner = () => {
    return (
        <div className={classes.spinner}></div>
    )
}

export default Spinner;
